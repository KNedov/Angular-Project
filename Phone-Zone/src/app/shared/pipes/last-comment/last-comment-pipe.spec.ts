import { LastCommentPipe } from './last-comment.pipe';

describe('LastCommentPipe', () => {
  it('create an instance', () => {
    const pipe = new LastCommentPipe();
    expect(pipe).toBeTruthy();
  });
});
