import { ActiveLink } from './active-link';

describe('ActiveLink', () => {
  it('should create an instance', () => {
    const directive = new ActiveLink();
    expect(directive).toBeTruthy();
  });
});
